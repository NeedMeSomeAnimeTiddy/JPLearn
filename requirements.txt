psutil
pytest
# Required: Japanese morphological tokenization (word/POS segmentation) used
# by data/text_normalization.py::tokenize_japanese and downstream parsing.
fugashi
unidic-lite
# Optional: offline Japanese speech-to-text for the speech-answer minigame mode.
# Heavy dependency (ctranslate2 + onnxruntime); only required if the user
# installs a speech recognition model from Setup or Settings.
faster-whisper
# Optional: local ONNX-based sentence embedder for assistant chat retrieval
# (scripts/embedder_runtime.py). Lightweight (~15-20 MB) compared to
# torch/transformers; only required if the mapped embedder model (see
# scripts/get_embedder_model.py) has been installed. Falls back to the
# dependency-free hashed embedder in domain/retrieval.py if not installed.
onnxruntime
tokenizers
# Optional: enables Hugging Face CLI (`hf`) for faster/resumable model downloads
# in scripts/hf_download.py when available.
huggingface-hub
# Python GUI dependencies were removed.
# Frontend is now Electron + React + TypeScript in electron-frontend/.
